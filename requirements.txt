# =============================================================================
# 稼働.com - 統合依存関係管理
# バッチ処理システム + APIサーバー + 開発ツール
# =============================================================================

# -----------------------------------------------------------------------------
# 🗄️ データベース（共通）
# -----------------------------------------------------------------------------
psycopg2-binary>=2.9.7
SQLAlchemy>=2.0.23

# -----------------------------------------------------------------------------
# 🌐 Web & API（server/用）
# -----------------------------------------------------------------------------
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# -----------------------------------------------------------------------------
# 📊 データ処理（batch/用）
# -----------------------------------------------------------------------------
aiohttp>=3.8.0
beautifulsoup4>=4.12.2
selenium>=4.15.0
webdriver-manager>=4.0.0
lxml>=4.9.3
requests>=2.31.0
pandas>=2.0.0

# -----------------------------------------------------------------------------
# ⏰ スケジューリング（batch/用）
# -----------------------------------------------------------------------------
APScheduler>=3.10.0
aiocron>=1.8

# -----------------------------------------------------------------------------
# 🛠️ ユーティリティ（共通）
# -----------------------------------------------------------------------------
PyYAML>=6.0.0
python-dotenv>=1.0.0
pytz>=2023.3

# -----------------------------------------------------------------------------
# 🧪 開発・テスト
# -----------------------------------------------------------------------------
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0

# -----------------------------------------------------------------------------
# 🚀 本番環境
# -----------------------------------------------------------------------------
gunicorn>=21.2.0  # 本番WSGI サーバー

# -----------------------------------------------------------------------------
# 📱 ブラウザ自動化（batch/用）
# -----------------------------------------------------------------------------
selenium>=4.15.2
chromedriver-autoinstaller>=0.6.2
aiohttp>=3.8.0
beautifulsoup4>=4.12.2
selenium>=4.15.0
webdriver-manager>=4.0.0
lxml>=4.9.3
requests>=2.31.0

# Scheduling
APScheduler>=3.10.0
aiocron>=1.8

# Date/time handling
pytz>=2023.3

# Data processing
pandas>=2.0.0  # 高度なデータ分析用（オプション）

# スクレイピング・HTMLデバッグ用
selenium>=4.15.2
chromedriver-autoinstaller>=0.6.2

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0

# System dependencies (システムパッケージマネージャーでインストール)
# - libpq-dev (Linux環境でpsycopg2コンパイル用)
# - python3-dev (ネイティブ拡張機能コンパイル用)

# フロントエンド用（将来的な追加）
# flask>=2.3.0
# gunicorn>=21.0.0
